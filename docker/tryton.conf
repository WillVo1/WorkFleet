# Tryton client profile config — pre-fills server, db, and user
# so the agent only needs to click "Connect" and enter the password.
[default]
profile = local

[profile local]
host = localhost
port = 8000
database = tryton
user = admin
